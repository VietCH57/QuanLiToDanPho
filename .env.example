# ==============================================
# CẤU HÌNH DATABASE - MYSQL
# ==============================================
# Copy file này thành .env và điền thông tin thật

DB_ENGINE=django.db.backends.mysql
DB_NAME=QLToDanPho
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_HOST=127.0.0.1
DB_PORT=3306

# ==============================================
# CẤU HÌNH DJANGO
# ==============================================
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================
# HƯỚNG DẪN SỬ DỤNG
# ==============================================
# 1. Copy file này: cp .env.example .env
# 2. Sửa DB_PASSWORD thành password MySQL của bạn
# 3. Cài python-dotenv: pip install python-dotenv
# 4. Cập nhật settings.py để đọc từ .env:
#
#    from dotenv import load_dotenv
#    import os
#    load_dotenv()
#
#    DATABASES = {
#        'default': {
#            'ENGINE': os.getenv('DB_ENGINE'),
#            'NAME': os.getenv('DB_NAME'),
#            'USER': os.getenv('DB_USER'),
#            'PASSWORD': os.getenv('DB_PASSWORD'),
#            'HOST': os.getenv('DB_HOST'),
#            'PORT': os.getenv('DB_PORT'),
#        }
#    }
